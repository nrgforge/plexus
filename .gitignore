/target

# llm-orc execution artifacts (transient, not tracked)
.llm-orc/artifacts/

# ONNX model cache (downloaded at runtime by fastembed)
.fastembed_cache/

# Git worktrees
.worktrees/

# Session artifacts (play DB, scratch notes — ephemeral, not tracked)
/session/
