{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://plexus.dev/schemas/structural-analysis-output.schema.json",
  "title": "Structural Analysis Output",
  "description": "Structured output from structural analysis passed to semantic extraction (via llm-orc). Produced by SemanticAdapter::build_input().",
  "type": "object",
  "required": ["file_path"],
  "properties": {
    "file_path": {
      "type": "string",
      "description": "Absolute or project-relative path to the source file."
    },
    "mime_type": {
      "type": "string",
      "description": "MIME type from registration (e.g., 'text/markdown', 'audio/mpeg')."
    },
    "sections": {
      "type": "array",
      "description": "Structural boundaries identified by structural analysis. Used by llm-orc for intelligent chunking (ADR-021).",
      "items": {
        "type": "object",
        "required": ["label", "start_line", "end_line"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Human-readable section label (e.g., 'Act I', '## Introduction')."
          },
          "start_line": {
            "type": "integer",
            "minimum": 1,
            "description": "First line of the section (1-indexed)."
          },
          "end_line": {
            "type": "integer",
            "minimum": 1,
            "description": "Last line of the section (1-indexed, inclusive)."
          }
        },
        "additionalProperties": false
      }
    },
    "existing_concepts": {
      "type": "array",
      "description": "Concept labels already discovered by registration + structural analysis. Helps semantic extraction avoid redundant extraction and anchor new concepts to existing ones.",
      "items": {
        "type": "string"
      }
    },
    "statistics": {
      "type": "object",
      "description": "File-level statistics from structural analysis.",
      "properties": {
        "word_count": {
          "type": "integer",
          "minimum": 0
        },
        "line_count": {
          "type": "integer",
          "minimum": 0
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
